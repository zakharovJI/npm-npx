import { IColor, IMedia } from "@/helpers/interfaces";
import { colors as colors_ } from "@/helpers/styleColors";
import {
  media as media_,
  mediaRevers as mediaRevers_,
} from "@/helpers/styleBreakpoints";

export const colors: IColor = colors_;
export const media: IMedia = media_;
export const mediaRevers: IColor = mediaRevers_;
