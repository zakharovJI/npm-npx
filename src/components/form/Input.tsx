import React from "react";
import styled from "styled-components";

const StyledInput = styled.div``;

const Input: React.VFC = (): JSX.Element => {

  return <StyledInput />;
};

export default Input;
