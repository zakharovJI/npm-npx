import React from "react";
import styled from "styled-components";

const StyledButton = styled.div``;

const Button: React.VFC = (): JSX.Element => {
  return <StyledButton />;
};

export default Button;
